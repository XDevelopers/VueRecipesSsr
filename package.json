{
  "name": "vuessr",
  "version": "1.0.0",
  "description": "My awe-inspiring Nuxt.js project",
  "author": "jan pungarsek",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run build && push-dir --dir=.nuxt/dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "bootstrap-vue": "^2.0.0-rc.11",
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
